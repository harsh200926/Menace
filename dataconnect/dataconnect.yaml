specVersion: "v1"
serviceId: "gw"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gw-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
